    <form action="/edit-game/{{game._id}}" method="post">
        <input type="text" name="title" placeholder="Title" value="{{game.title}}">
        <input type="text" name="genre" placeholder="Genre" value="{{game.genre}}">
        <textarea id="description" name="description" rows="6" placeholder="Description" value="{{game.description}}"></textarea>
        <input type="submit" class="btn">
    </form>


<form action="/delete-game/{{game._id}}" method="post">
    <input type="submit" value="Delete" class="btn delete-btn">
</form>
